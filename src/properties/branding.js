// Logo della proprietà attiva: src/properties/<id>/assets/branding/logo.png
const logo = require(`./${process.env.REACT_APP_PROPERTY_ID || "casa-nei-pini"}/assets/branding/logo.png`);

export default logo;
